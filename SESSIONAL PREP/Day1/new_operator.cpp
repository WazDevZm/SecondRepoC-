#include<iostream>

using namespace std;

int main() {
    // clearly showing the application of the new operator in C++
    int* nptr;
    nptr = new int(90);
    cout<<*nptr<<endl; // prints the value stored at the address pointed to by nptr, which is 90
    cout<<nptr<<endl; // prints the address stored in nptr

    




    return 56;
}